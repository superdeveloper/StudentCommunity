<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp">

    <CheckBox
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:id="@+id/search_result_add_check"
        android:layout_marginLeft="10dp"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:visibility="invisible"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"/>

        <TextView
            android:id="@+id/search_result_with_add_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?android:listPreferredItemHeightSmall"
            android:gravity="start"
            android:paddingTop="15dp"
            android:paddingBottom="@dimen/spacing_normal"
            android:paddingRight="@dimen/padding_normal"
            android:paddingEnd="@dimen/padding_normal"
            android:textAlignment="viewStart"
            android:elevation="1dp"
            android:layout_toEndOf="@+id/search_result_add_check" />

    <!--<TextView
        android:id="@+id/list_item_search_result_add_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:gravity="center"
        android:textSize="@dimen/app_text_size_xsmall"
        android:padding="@dimen/spacing_normal"
        android:paddingTop="@dimen/spacing_micro"
        android:paddingBottom="@dimen/spacing_micro"
        android:text="+"
        style="@style/MoreButton"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true" />-->
    </RelativeLayout>
</LinearLayout>